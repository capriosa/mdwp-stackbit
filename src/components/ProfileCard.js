import Fade from 'react-reveal/Fade';
import React from 'react';

const ProfileCard = ({ annotationPrefix, title, body, testimonial }) => {
    return (
        
    );
};

export default ProfileCard;
